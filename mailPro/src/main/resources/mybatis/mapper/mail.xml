<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.mail.mapper.MailMapper">
	
	<insert id="insertJamesUser" parameterType="JamesUserDTO">
		INSERT INTO JAMES_USER
			(USER_NAME, PASSWORD_HASH_ALGORITHM, PASSWORD, VERSION)
		VALUES
			(#{userName}, 'NONE', #{password}, 1)
	</insert>
	
	<select id="selectJamesUserByEmpNo" parameterType="String" resultType="JamesUserDTO">
		SELECT USER_NAME, PASSWORD_HASH_ALGORITHM, PASSWORD, VERSION
		  FROM JAMES_USER
		 WHERE USER_NAME = #{userName}
	</select>

</mapper>